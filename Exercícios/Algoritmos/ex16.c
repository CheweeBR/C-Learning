#include <stdio.h>
int main() {
        float km, m;
        printf("Velocidade (km/h): ");
        scanf("%f", &km);
        m = km / 3.6;
        printf("%.0f Km/h equivale a %.0f m/s",km,m);
        return 0;
        //chewee was here =]
}